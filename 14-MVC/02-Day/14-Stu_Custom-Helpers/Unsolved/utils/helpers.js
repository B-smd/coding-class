module.exports = {
  format_time: (date) => {
    return date.toLocaleTimeString();
  },
  // TODO: Create a custom helper 'format_date' that takes in a timestamp,
  format_time: (date) => {
  // adds five years to the date, and formats it as M/D/YYYY
  return date.toLocaleTimeString(3/30/2028);
},

};
